# /ai-enabled-delivery → /ai-platform (B1 deliverable)
# 当 B1 (W1) 上线后取消注释下行
/ai-enabled-delivery /ai-platform 301

# /ai-platform v3.0 子页 — 200 rewrite 把 nested URL 映射到扁平 HTML
# (Plan v1.2 task 12 修订: 不引 Next.js 动态路由, 用扁平 HTML + rewrite)
# 浏览器地址栏保持 /ai-platform/<slug>; 静态托管层做透明 rewrite。
/ai-platform/deepevidence  /ai-platform-deepevidence.html  200
/ai-platform/seekevidence  /ai-platform-seekevidence.html  200

# W4 launch 切换:
# 当前 /ai-platform 仍指 ai-platform.html (v2);
# Task 19 launch day 把 ai-platform-v3.html 重命名为 ai-platform.html, 并把
# 旧 v2 移到 _archive/。 在切换前预览 v3 总览页可访问 /ai-platform-v3 (不挂 redirect)。

# IA §6 历史保留 URL（旧外链 SEO 继承）
# 仅在目标 URL 已上线后启用，否则会 301 到 404

# W0 即可启用（目标在既有 6 solutions 页中）：
/china                /solutions/entering-china     301
/usa                  /solutions/going-global-us    301
/us                   /solutions/going-global-us    301

# W5 (B5) 上线 /pilots/china-evidence-sprint 后取消注释下行：
# /pilot                /pilots/china-evidence-sprint 301
