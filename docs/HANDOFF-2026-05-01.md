# MSH Website Rebuild — Session Handoff (2026-05-01)

> Hand-off written at the end of a long working session. The next agent should
> read this end-to-end before touching code. The previous handoff is at
> `docs/HANDOFF-2026-04-28.md` — this doc supersedes it for everything
> that landed afterwards.

## 1 · Where we are right now

**Branch.** `UXcritique20260429` is the active working branch; HEAD is
`1bb3dae`. Last 3 commits on this branch:

```
1bb3dae feat(homepage): physician-network map + Cases preview strips
b245ca6 feat(ai-platform): add DeepInsight as 3rd product + v4 engineering-spec variant
d2142d7 AIProductShowcase: replace near-black hex with brand-spec navy tokens
```

Everything described below is committed. There are NO outstanding
edits in `prototype/`. There ARE many untracked legacy files in the
project root (W1-XX, W2-XX, docs, assets) that pre-date this branch
— do not stage them unless explicitly asked.

**Project context (from project instructions).** 梅斯健康 (HKEX 2415)
官网重构。两个并行目标:
1. 美国业务扩张 + 国际投资并购**反向尽调** — 站点要读起来像
   audit-grade evidence platform
2. AI 时代的全新品牌形象 — restrained, physician-verified, not "AI-native
   playground"

**Tech stack (locked decisions).**
- In-browser React 18 + Babel JSX, NO bundler (script tags load `.jsx` via Babel)
- All components in `prototype/components/*.jsx`; each registers itself on `window.<Name>`
- Single-hue blue + cyan brand (per `W1-04 Brand Guidelines.md` + `assets/colors_and_type.css`)
- Display font: **Footlight MT Light** (user-locked override of spec — spec said Source Serif 4)
- UI font: Inter
- Mono: ui-monospace stack
- All numbers in Inter per spec §3.4
- Sanity CMS schemas exist under `W2-05 Sanity Schema v1.0/` but are NOT wired
  to the prototype yet (mock data lives inline in the JSX)
- GA4 + consent-mode v2 wired (commit `e3e735b`)

## 2 · What this session shipped

### 2.1 AI Platform — DeepInsight + v4 engineering-spec (commit b245ca6)

Two pages now coexist for `/ai-platform`:

**`prototype/ai-platform-v3.html`** — chat-UI restrained variant.
- Uses `AIProductShowcase` (tab carousel with browser-mockup demo) + `AIProductCardGrid`.
- Layout: Hero → Foundation (`AIAssetBalanceSheet`) → Products (Showcase + CardGrid) → Method (`TrustArchitecture`) → CTA.

**`prototype/ai-platform-v4.html`** — engineering-spec variant.
- Replaces showcase + grid with a single `AIProductSpec` component.
- Each product renders as a stacked block: header → capabilities → input/output contract → citation strip → status pill + CTA.
- v4 reads as API contract / spec sheet, not an AI playground.

**Three products** (same `PRODUCTS` array shape in both v3 and v4):
1. **DeepEvidence** — `accentColor: 'navy'`, `limitedPreview`, Clinical AI · Point of Care
2. **SeekEvidence** — `accentColor: 'violet'` ⚠️ **brand carve-out**, `limitedPreview`, Research AI · Evidence Synthesis
3. **DeepInsight** — `accentColor: 'cyan'`, `inDevelopment`, Industry AI · Market Signal & Insight (no `showcase.demoScenario`)

The violet carve-out for SeekEvidence is **user-locked** to match the
already-shipped `seekevidence-web.medsci.cn`. Do not normalize it back
to navy/cyan without explicit instruction.

**v3 product filter:** `<AIProductShowcase products={PRODUCTS.filter(p => p.showcase && Array.isArray(p.showcase.demoScenario))} />` — DeepInsight has no demoScenario, so it does NOT appear as a tab; it only renders in the CardGrid below.

**v4 inDevelopment handling:** `AIProductSpec` skips the input/output cells when no demoScenario exists, replaces them with a dashed-border "Roadmap · in development" note, and swaps the CTA to outline-style "Become a design partner →" pointing to `intent=ai_design_partner`.

**Shared component changes:**
- `AIProductCard.jsx`: added `inDevelopment` status (warning amber palette); hides "Request access" CTA for inDevelopment, shows cyan "Become a design partner →" instead
- `AIAssetBalanceSheet.jsx`: 2-stack groups → 4-up flat grid, theme inverted to light bg-2, ~35-45% vertical compression
- `AIProductShowcase.jsx`: brand-aligned palette (navy + cyan + violet, was cross-hue), `ProductIcon` helper with SeekIconSvg/ActivityIconSvg ports from `_reference/medsci-evidence-tech`, mockup height bumped to `clamp(600px, 76vh, 760px)` to kill demo scrollbar
- `AIProductShowcaseDemo.jsx`: removed `overflowY: 'auto'` and `maxHeight: '100%'`
- `StickyAnchorNav.jsx`: `sticky` prop default `false` (removes nav overlap on scroll for v3/v4)
- `ComplianceCallout` REMOVED from v3/v4 (redundant with TrustArchitecture's three-pillar framing)

**Decision still pending:** user has not chosen which (v3 or v4) becomes canonical at `/ai-platform.html`. Both currently coexist as separate URLs. There's also a legacy `prototype/ai-platform.html` that pre-dates the v3/v4 split — leave it alone for now.

### 2.2 Homepage — physician network map + Cases preview strips (commit 1bb3dae)

**`PhysicianNetworkMap.jsx`** (new) — section inserted between `<WhyMedSci />` and `<Cases />` on homepage.

- Pure-SVG dot-density visualization; ZERO external geo libraries
- Zoomed to central+eastern China only (lng 99-126°E, lat 18-44°N) — drops Xinjiang, Tibet, far Heilongjiang
- Hand-simplified outlines: China ~163 pts + Hainan + Taiwan; rendered as low-opacity navy fill + thin stroke
- 33 city dots sized by physician count, 6 emphasis hubs labelled (Beijing, Shanghai, Hangzhou, Wuhan, Chengdu, Guangzhou)
- 10 inter-hub connection lines (intra-China only — US inset was REMOVED at user request)
- Density dots scattered with mulberry32 seed=2415 (HKEX ticker) → deterministic render
- Section copy: 4 inline stats (3.33M+ / 25K+ / 31 / 6 hubs), legend (density / hub / link)
- SVG has `overflow: hidden` so polygon edges clip cleanly at viewBox

**`CardPreviewStrip.jsx`** (new) — inline-SVG mini-viz at top of cards.

- Single component, switch-dispatched on `kind` prop
- 130px tall, full-bleed via negative margin (`-28` to escape card padding)
- Card root needs `overflow: hidden` + `borderRadius` for clean top corners
- Brand-disciplined: navy + cyan only, no shadows, no animation

**Wired on Cases (3 cards):**
| Case | `previewKind` | Visual |
|---|---|---|
| US device → NMPA, 11 wks | `case-timeline-11wk` | 4-segment phase ribbon, "0 RFIs" corner |
| 38 bilingual artifacts, 96% QC | `case-passrate-38` | 38-dot grid + 96% callout |
| 42→18 day response | `case-deltabars-42-18` | Before/after horizontal bars + delta % |

**Insights cards:** the dispatcher defines 3 dormant kinds (`insight-bars-nmpa`, `insight-funnel-bridge`, `insight-bilingual-pair`) that I built and the user later **reverted** — Insights cards are pure-text again. The dormant kinds are kept in `CardPreviewStrip.jsx` for easy re-enable; do NOT delete them.

## 3 · Image-add strategy (long-term plan)

User asked about adding images to a currently image-free site. The
agreed strategy lives in chat history, summarized here so the next
session does not have to re-litigate it:

- The "no images" state was deliberate (audit-grade aesthetic), not a placeholder
- 4 categories of imagery earn their place; in priority:
  1. **Real product UI screenshots** — replace the chat-UI mockup in `AIProductShowcase` with actual NovaX-Pro / SeekEvidence screenshots. ⚠️ User explicitly said **defer** this for now.
  2. **Editorial data visualizations** — physician network map (DONE), case-study hero charts (forest plots / KM curves), insight thumbnails. ⚠️ User chose to revert Insights strips; Cases strips landed.
  3. **Real people, selectively** — leadership headshots (`LeadershipGrid` component already has placeholder slots), Insights article reviewer mini-avatars, footer "Reviewed by" mini-avatars.
  4. **Place, exactly one** — about/contact gets ONE office or HQ photo, no more.

**Hard rule:** no stock photos, no smiling-doctor cliché, no
glassmorphism, no decorative AI-generated illustrations. If we add an
image, it must be referenceable (real screenshot / real data / real
person / real building).

**Per-page placement table** (from session history; do not re-derive):

| Page | Where | Type | Status |
|---|---|---|---|
| Homepage hero | n/a | network viz | Done as separate section, not in Hero |
| Homepage Cases 3 cards | top strip | de-identified result chart | Done (3 strips) |
| Homepage Insights 3 cards | top strip | feature data chart | **Reverted** — pure text |
| AI Platform demo window | replace mockup | real product UI | **Deferred** by user |
| AI Platform foundation | next to balance sheet | knowledge-graph anchor diagram | Not started |
| Case study detail hero | top of detail page | result chart | Not started |
| Insights detail end | reviewer block | named physician avatar 28×28 | Not started |
| About / Leadership | team grid | unified headshots | Component exists, no images |
| About / Contact | section | one office / HQ photo | Not started |

## 4 · User preferences (sticky across sessions)

- **Brief explanations.** Configured in user_preferences. Do not write 5-paragraph diagnoses.
- **Markdown output.** Project instructions explicitly say generated docs use `markdown` format (the project file says `makedown` — same intent).
- **Footlight MT** as display font — keep it. Spec says Source Serif 4 but user reversed that.
- **SeekEvidence violet** — keep it. Spec says single-hue blue but user locked the violet carve-out.
- **Chinese for chat, English for production copy** — most artifact text is English; conversational answers can mix.
- **No emojis** unless user uses them first.
- **Sandbox cannot write to `.git/`** — every commit needs to be run manually by the user in their local terminal. Provide ready-to-paste commands. If a `.git/index.lock` shows up, it's a stale file from a sandbox attempt; user runs `rm -f .git/index.lock` to clear.

## 5 · Codebase landmarks

```
prototype/
├── index.html                          ← homepage
├── ai-platform.html                    ← legacy, untouched recently
├── ai-platform-v3.html                 ← chat-UI restrained variant
├── ai-platform-v4.html                 ← engineering-spec variant
├── ai-platform-deepevidence.html       ← deep-link subpages (DE/SE)
├── ai-platform-seekevidence.html
├── about.html, contact.html, 404.html, ...
├── _reference/medsci-evidence-tech/    ← READ-ONLY, source for icon ports etc
├── assets/
│   ├── colors_and_type.css             ← all CSS variables live here
│   ├── responsive.css
│   ├── fonts/, logo/, vendor/, analytics/, consent/
└── components/
    ├── Header.jsx, Hero.jsx, Footer.jsx
    ├── Sections1.jsx (TwoPaths/Services/QuickStart)
    ├── Sections2.jsx (WhyMedSci/Cases — CaseCard now uses CardPreviewStrip)
    ├── Sections3.jsx (AISection/TrustBar/Insights/FinalCTA)
    ├── PhysicianNetworkMap.jsx        ← homepage map (NEW, this session)
    ├── CardPreviewStrip.jsx           ← homepage Cases strips (NEW, this session)
    ├── AIAssetBalanceSheet.jsx        ← /ai-platform Foundation block
    ├── AIProductShowcase.jsx          ← v3 chat-UI tabs
    ├── AIProductShowcaseDemo.jsx      ← v3 mockup body
    ├── AIProductCard.jsx, AIProductCardGrid.jsx ← v3 grid below tabs
    ├── AIProductSpec.jsx              ← v4 stacked engineering-spec block
    ├── ComingSoonCard.jsx             ← unused on v3 now (DeepInsight took its slot)
    ├── TrustArchitecture.jsx          ← /ai-platform Method block
    ├── ComplianceCallout.jsx          ← REMOVED from v3/v4 but file kept
    ├── PitlRibbon.jsx, EvidenceTrail.jsx, MetricStrip.jsx ← signature components
    └── ... many more
```

**CSS variables you will use most often** (from `assets/colors_and_type.css`):
- `--brand-primary-700` (navy 900-ish)
- `--brand-primary-900` (deeper navy, hero bg)
- `--brand-accent-500` (cyan)
- `--brand-accent-700` (deeper cyan, headings on light bg)
- `--bg-1` (white), `--bg-2` (#fafbfc-ish), `--fg-1/2/3` (text gradient)
- `--border-1`, `--border-2`
- `--font-display` (Footlight MT), `--font-ui` (Inter), `--font-mono`

## 6 · Open / deferred items

In rough priority order:

1. **Choose v3 or v4 as canonical `/ai-platform.html`.** Both pages exist; user has not picked one. Likely v3 (chat-UI is more on-brand for the demo audience), but defer until user weighs in.
2. **Real product UI screenshots in AI Platform demo windows** — user said "暂时不换". Wait for explicit approval before touching `AIProductShowcaseDemo`.
3. **Leadership headshots in `LeadershipGrid`** — component scaffolded, awaiting real photos from user.
4. **Case study detail-page hero charts** — recommended but not started.
5. **About / Contact office photo** — recommended but not started.
6. **Sanity CMS wiring** — schemas exist under `W2-05 Sanity Schema v1.0/`, prototype still uses inline mock data. Big task; not in scope for image-add work.
7. **Network map polish (optional)** — outline at 163 pts is good but user could ask for further refinement (rooster-comb NE, Yangtze delta, etc). Don't start unprompted.

## 7 · Recommended handoff prompt for the new session

Paste this at the start of the new session:

> 接手 MSH 官网重构项目。先阅读 `/Users/yogyoung/Documents/MSH-website-rebuild/MSH Website Rebuild/docs/HANDOFF-2026-05-01.md` 了解当前状态、品牌纪律、用户锁定决策、开放任务。读完之后等我下一条具体指令。不要主动展开任何工作。

That's it. The doc above carries all the context. Keep this prompt
short — the agent will Read the doc and have everything it needs.

## 8 · Things the next agent must NOT do

- Do not auto-stage `W1-XX`, `W2-XX`, `docs/superpowers/`, `assets/`, or `_reference/` even though they show as untracked. Those are legacy / read-only material.
- Do not change Footlight MT to Source Serif 4. User locked Footlight MT.
- Do not normalize SeekEvidence violet to brand-spec navy/cyan. User locked the violet carve-out.
- Do not delete the dormant `insight-*` kinds in `CardPreviewStrip.jsx`. They are reserved for re-enable.
- Do not commit from the sandbox — it cannot write to `.git/`. Always hand the user commands.
- Do not introduce stock photos, glassmorphism, gradients on data, or per-card decorative color carve-outs (other than SeekEvidence violet).
- Do not delete `prototype/ai-platform.html` (the legacy) yet — it's the current canonical until v3/v4 winner is chosen.
- Do not regenerate `PRODUCTS` array structure in v3/v4 separately — they share schema; if you change one, mirror to the other.

— end of handoff —
